# Database Configuration
# Connection pooling is configured in DATABASE_URL query parameters
# Pool size: connection_limit (default: calculated by Prisma)
# Pool timeout: pool_timeout (default: 10s)
# Example with custom pooling:
#   DATABASE_URL="postgresql://user:password@localhost:5432/pelaris?connection_limit=20&pool_timeout=20"
DATABASE_URL="postgresql://user:password@localhost:5432/pelaris"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# Server Configuration
PORT=5100
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS="http://localhost:3100,http://localhost:4000,http://127.0.0.1:3100,http://127.0.0.1:4000"

# Additional production domain (optional - ziqrishahab.com already allowed by default)
# Only set this if you want to allow OTHER domains besides ziqrishahab.com
PRODUCTION_DOMAIN=""

# Backup Configuration
BACKUP_DIR="./backups"
BACKUP_RETENTION_DAYS=7

# Socket.io Configuration
SOCKET_CORS_ORIGIN="http://localhost:3100"

# Rate Limiting Configuration (optional - defaults below)
# In development: limits are 10x more lenient automatically
# Window time: 5 minutes (300000ms) for faster reset
# RATE_LIMIT_WINDOW_MS=300000    # 5 minutes in ms (was 15 min)
# RATE_LIMIT_MAX_REQUESTS=100    # Max requests per window
# RATE_LIMIT_LOGIN_MAX=10        # Max login attempts (100 in dev)
# RATE_LIMIT_REGISTER_MAX=5      # Max register attempts (50 in dev)

# Redis Configuration (optional - uses in-memory if not configured)
# Format: redis://[:password@]host[:port][/db-number]
# Examples:
#   REDIS_URL=redis://localhost:6379
#   REDIS_URL=redis://:mypassword@localhost:6379
#   REDIS_URL=redis://redis-host:6379/0
REDIS_URL=""

# Logging Configuration
# LOG_LEVEL=info                 # Levels: error, warn, info, debug
# Valid levels: error (0), warn (1), info (2), http (3), verbose (4), debug (5)

# Sentry Error Monitoring (optional)
# Get your DSN from https://sentry.io/
SENTRY_DSN=""
SENTRY_ENABLED=false            # Set to true to enable in development
